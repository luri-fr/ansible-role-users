---
# vars file for users

_users_requirements:
  default: []
  Amazon:
    - shadow
  Suse:
    - shadow

users_requirements: "{{ _users_requirements[ansible_os_family] | default(_users_requirements['default']) }}"
